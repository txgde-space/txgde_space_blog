---
title: '[学习笔记]计算机系统学习笔记Week2-1'
date: '2022-07-18 23:09:06'
draft: false
categories: ['学习笔记']
tags: ['ASCII', 'IEEE', '学习笔记', '学习笔记', '浮点数', '计算机系统']
---

## 浮点数的规格化表示

规定尾数首位为1

### IEEE 754标准

#### 32位(单精度)

1位符号位+8位阶码+23位尾数(其中尾数第一位总为1，因此可以省一位)

#### 64位(双精度)

1位符号位+11位阶码+52位尾数(其中尾数第一位总为1，因此可以省一位)

#### 特殊数的表示

##### 0

**阶码和尾数全部为0**，符号位为1表示-0，为0表示+0

##### +∞和-∞

**阶码全为1**，**尾码全为0**，符号位表示正负

注：浮点数除以0不会发生异常，而是会得到无穷， X/0>Y，X,Y>0

##### NaN(Not a Number)非数

阶码全为1，尾数不为0

例：sqrt(-4.0)=NaN

##### 规格化数

阶码为1-254，尾数任意

##### 非规格化数

阶码全为0，尾数不为0

## 逻辑数据的表示

逻辑值:真/假

### 表示

用一位表示。N位二进制数可表示N个逻辑数据

### 运算

按位进行

### 识别

在形式上与其他数据并无差别，需要通过指令来识别

## 西文数据的表示

### 特点

* 是一种拼音文字，用有限几个字母可拼写出所有单词
* 只需对有限个字母和数学符号、标点符号等辅助字符编码
* 所有字符总数不超过256个，使用7或8个二进位可表示

### 表示

常用7位ASCII码来表示

![img](https://www.runoob.com/wp-content/uploads/2022/03/ascii-1-1.png)

## 汉字及国际字符数据的表示

### 特点

汉字是表意文字，一个字就是一个方块图形。 汉字数量巨大，总数超过6万字，给汉字在计算机内部的表示、汉字的传输与交换、汉字的输入和输出等带来了一系列问题。

### 编码形式

**输入码**∶对汉字用相应按键进行编码表示，用于输入

**内码**:用于在系统中进行存储、查找、传送等处理 **字模点阵或轮廓描述**:描述汉字字模点阵或轮廓，用于显示/打印

#### GB2312国标码

GB2312区位码由码表的行和列组成，各占七位，在行和列前面加个0便组成了两个字节，将每个字节的最高位置为"1"来区分ASCII码。

## 多媒体信息的表示

**图形**用构建图形的直线或曲线的坐标点及控制点来描述，而这些坐标点或控制点则用数值数据描述 **图像**用构成图像的点(像素)的亮度、颜色或灰度等信息来描述，这些亮度或颜色等值则用数值数据描述 **音频**信息通过对模拟声音进行采样、量化(用二进制编码）来获得，因此量化后得到的是一个数值数据序列（随时间变化)

**视频**信息描述的是随时间变化的图像(每一幅图像称为一帧)

**音乐**信息(MIDI)通过对演奏的乐器、乐谱等相关的各类信息用0和1进行编码来描述
